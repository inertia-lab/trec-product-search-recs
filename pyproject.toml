[project]
name = "trec-product"
version = "0.1.0"
description = "Tools for the TREC product search & recommendation track."
readme = "README.md"
requires-python = "~=3.12.0"
dependencies = [
  "docopt-ng ~=0.9",
  "duckdb ~=1.3",
]
